
import {useNowPlaying} from '../hooks/useNowPlaying';
import {usePopular} from '../hooks/usePopular';
import {useTopRated} from '../hooks/useTopRated';
import {useUpComing} from '../hooks/useUpComing';

export const moviesHooks = {
    useNowPlaying,
    usePopular,
    useTopRated,
    useUpComing
}